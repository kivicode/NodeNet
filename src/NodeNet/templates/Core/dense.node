<%
Dense
@color(0, 122, 216)
input name=Units type=int
input name=Activation type=string_list options={relu,softmax}
%>

input("Name") = Dense(input("Units"), activation="input("Activation")")(input("Link"))

